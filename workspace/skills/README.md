# workspace/skills

Legacy pointer: active runtime configuration now lives in `workspace/agents/`.

Use `workspace/agents/<name>/AGENT.md` plus optional `workspace/agents/<name>/context/*.md`, then reference the agent from automations with `agent: <name>`.
