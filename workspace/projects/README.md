# jumpyGoatHq projects moved to boards

`projects/` has been renamed to `boards/` before release.

Use:

```txt
boards/<board>/BOARD.md
boards/<board>/tasks/<task-id>.md
```

See `workspace/boards/README.md` for the current contract.
