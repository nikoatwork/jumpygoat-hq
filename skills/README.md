# skills

Legacy pointer: customer-facing runtime configuration has moved to agents.

Use `workspace/agents/<name>/AGENT.md` (or `$AGENTHQ_HOME/agents/<name>/AGENT.md`) and reference it from automations with `agent: <name>`.
